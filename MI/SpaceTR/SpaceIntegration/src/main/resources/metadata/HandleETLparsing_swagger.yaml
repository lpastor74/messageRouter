openapi: 3.0.1
info:
  title: MessageRouting
  description: API Definition of HandleETLparsing
  version: 1.0.0
servers:
- url: http://localhost:8290/message
paths:
  /createIssues:
    post:
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
        required: false
      responses:
        default:
          description: Default response
